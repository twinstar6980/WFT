inputs:
  platform:
    description: platform
    required: true
runs:
  using: composite
  steps:
    - name: check
      shell: pwsh
      run: |
        flutter doctor -v
        tsc --version
        xmake --version