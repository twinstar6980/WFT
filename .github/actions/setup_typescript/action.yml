inputs:
  platform:
    description: platform
    required: true
  version:
    description: version
    required: true
runs:
  using: composite
  steps:
    - name: install
      shell: pwsh
      run: |
        npm install -g typescript@${{ inputs.version }}
    - name: check
      shell: pwsh
      run: |
        tsc --version
