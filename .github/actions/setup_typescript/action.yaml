inputs:
  platform:
    description: platform
    required: true
  version:
    description: version
    required: true
runs:
  using: composite
  steps:
    - name: install
      shell: pwsh
      run: |
        npm list -g
        npm install -g typescript@${{ inputs.build_version }}"
    - name: check
      shell: pwsh
      run: |
        npm list -g
        tsc --version