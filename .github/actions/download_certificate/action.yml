inputs:
  source_file_pfx:
    description: source_file_pfx
    required: true
  source_password_pfx:
    description: source_password_pfx
    required: true
  source_file_jks:
    description: source_file_jks
    required: true
  source_password_jks:
    description: source_password_jks
    required: true
  destination:
    description: destination
    required: true
runs:
  using: composite
  steps:
    - name: download
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path "${{ inputs.destination }}"
        Set-Content -AsByteStream -Path "${{ inputs.destination }}/file.pfx" -Value ([System.Convert]::FromBase64String("${{ inputs.source_file_pfx }}"))
        Set-Content -Encoding utf8 -Path "${{ inputs.destination }}/password.pfx.txt" -Value ("${{ inputs.source_password_pfx }}")
        Set-Content -AsByteStream -Path "${{ inputs.destination }}/file.jks" -Value ([System.Convert]::FromBase64String("${{ inputs.source_file_jks }}"))
        Set-Content -Encoding utf8 -Path "${{ inputs.destination }}/password.jks.txt" -Value ("${{ inputs.source_password_jks }}")
    - name: check
      shell: pwsh
      run: |
        Get-ChildItem -Recurse -Path "${{ inputs.destination }}"
