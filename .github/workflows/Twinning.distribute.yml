name: Twinning - distribute
on:
  workflow_call:
    inputs:
      repository_url:
        required: true
        type: string
      enable_certificate:
        required: true
        type: boolean
      enable_webdav:
        required: true
        type: boolean
      target_platform:
        required: true
        type: string
    secrets:
      certificate_file_pfx:
        required: false
      certificate_password_pfx:
        required: false
      certificate_file_jks:
        required: false
      certificate_password_jks:
        required: false
      webdav_endpoint:
        required: false
      webdav_username:
        required: false
      webdav_password:
        required: false
env:
  host_platform: |-
    ${{ fromJSON('{
      "windows.amd64": "windows",
      "linux.amd64": "linux",
      "macintosh.arm64": "macintosh",
      "android.arm64": "windows",
      "iphone.arm64": "macintosh",
    }')[inputs.target_platform] }}
jobs:
  main:
    runs-on: |-
      ${{ fromJSON('{
        "windows.amd64": "windows-2025-vs2026",
        "linux.amd64": "ubuntu-24.04",
        "macintosh.arm64": "macos-26",
        "android.arm64": "windows-2025-vs2026",
        "iphone.arm64": "macos-26",
      }')[inputs.target_platform] }}
    steps:
      - name: check out
        if: ${{ true }}
        uses: actions/checkout@v4
      - name: setup python
        if: ${{ inputs.target_platform == 'windows.amd64' || inputs.target_platform == 'linux.amd64' || inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'android.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_python
        with:
          host_platform: ${{ env.host_platform }}
          version: '3.14.3'
      - name: setup windows-sdk
        if: ${{ inputs.target_platform == 'windows.amd64' }}
        uses: ./.github/actions/setup_windowssdk
        with:
          host_platform: ${{ env.host_platform }}
          version: '26100'
      - name: setup android-sdk
        if: ${{ inputs.target_platform == 'android.arm64' }}
        uses: ./.github/actions/setup_androidsdk
        with:
          host_platform: ${{ env.host_platform }}
          version: '36.1'
          build_version: '36.1.0'
          ndk_version: '29.0.14206865'
      - name: setup llvm
        if: ${{ inputs.target_platform == 'windows.amd64' || inputs.target_platform == 'linux.amd64' || inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_llvm
        with:
          host_platform: ${{ env.host_platform }}
          version: '21'
          windows_version: '21.1.8-20251216'
      - name: setup xmake
        if: ${{ inputs.target_platform == 'windows.amd64' || inputs.target_platform == 'linux.amd64' || inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'android.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_xmake
        with:
          host_platform: ${{ env.host_platform }}
          version: '3.0.7'
      - name: setup nodejs
        if: ${{ inputs.target_platform == 'windows.amd64' || inputs.target_platform == 'linux.amd64' || inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'android.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_nodejs
        with:
          host_platform: ${{ env.host_platform }}
          version: '25.8.1'
      - name: setup flutter
        if: ${{ inputs.target_platform == 'windows.amd64' || inputs.target_platform == 'linux.amd64' || inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'android.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_flutter
        with:
          host_platform: ${{ env.host_platform }}
          version: '3.41.5'
      - name: setup msbuild
        if: ${{ inputs.target_platform == 'windows.amd64' }}
        uses: ./.github/actions/setup_msbuild
        with:
          host_platform: ${{ env.host_platform }}
          version: '18.3.0'
      - name: setup xcode
        if: ${{ inputs.target_platform == 'macintosh.arm64' || inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_xcode
        with:
          host_platform: ${{ env.host_platform }}
          version: '26.3'
      - name: setup create-dmg
        if: ${{ inputs.target_platform == 'macintosh.arm64' }}
        uses: ./.github/actions/setup_createdmg
        with:
          host_platform: ${{ env.host_platform }}
          version: 'latest'
      - name: setup ldid
        if: ${{ inputs.target_platform == 'iphone.arm64' }}
        uses: ./.github/actions/setup_ldid
        with:
          host_platform: ${{ env.host_platform }}
          version: 'latest'
      - name: clone repository
        if: ${{ true }}
        shell: pwsh
        run: |
          git clone "${{ inputs.repository_url }}" "${{ github.workspace }}/repository"
      - name: download certificate
        if: ${{ inputs.enable_certificate }}
        uses: ./.github/actions/download_certificate
        with:
          source_file_pfx: ${{ secrets.certificate_file_pfx }}
          source_password_pfx: ${{ secrets.certificate_password_pfx }}
          source_file_jks: ${{ secrets.certificate_file_jks }}
          source_password_jks: ${{ secrets.certificate_password_jks }}
          destination: ${{ github.workspace }}/repository/.local/certificate
      - name: build repository
        if: ${{ true }}
        shell: pwsh
        run: |
          python3 "${{ github.workspace }}/repository/BUILD.py" "${{ inputs.target_platform }}"
      - name: upload to webdav
        if: ${{ inputs.enable_webdav }}
        uses: ./.github/actions/upload_file
        with:
          path: ${{ github.workspace }}/repository/.local/distribution/${{ inputs.target_platform }}.bundle.zip
          target: webdav
          webdav_endpoint: ${{ secrets.webdav_endpoint }}
          webdav_username: ${{ secrets.webdav_username }}
          webdav_password: ${{ secrets.webdav_password }}
          webdav_destination: '/Twinning'
      - name: upload to action
        if: ${{ true }}
        uses: ./.github/actions/upload_file
        with:
          path: ${{ github.workspace }}/repository/.local/distribution/${{ inputs.target_platform }}.bundle.zip
          target: action
          action_destination: ${{ inputs.target_platform }}.distribution
